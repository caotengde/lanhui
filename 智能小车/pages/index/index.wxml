<!--index.wxml
  40.138000,116.264110

<map id="map"
 longitude="{{log}}"
 latitude="{{lat}}"
 controls="{{controls}}" 
 markers="{{markers}}" 
 scale="17" 
 show-location 
 style="width: 100%; height:100%"
 bindcontroltap="controltap"
 bindregionchange="regionchange"
></map>
-->

<view class='container'>
  <map id="map"
    latitude="{{lat}}"
    longitude="{{log}}"
    controls="{{controls}}" 
    markers="{{markers}}" 
    scale="17" 
    show-location 
    style="width: 100%; height:100%"
    bindcontroltap="controltap"
    bindregionchange="regionchange"
    ></map>
    <navigator style="display:{{isHidden}}" class="promotion">
    <image src="../../images/mywallet.png"></image>
    <text>您有一个包裹正在配送中</text>
    <text class="right">></text>
  </navigator>
</view>
